class Node:
    def __init(self,val):
        self.val = val
        self.next = None

    
def iterative_linked(head):
    current = head
    while current != None:
        return current.val
        
        current = current.next
